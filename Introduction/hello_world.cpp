#include <iostream> // preprocessor directive -> we would like to use the
                    // contents of the iostream lib

                
int main() // int declares the type of the what is returned
{
    std::cout << "Hello world!";
    return 0;
}
